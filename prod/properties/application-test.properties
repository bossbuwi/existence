### Liquibase Configuration
# Path where the database changelogs are located
spring.liquibase.change-log= classpath:/db/changelog/changelog-master.xml
# Controls liquibase log level
logging.level.liquibase= INFO

### PostgreSQL Database Connection
# URL of the Postgres DB
spring.datasource.url= jdbc:postgresql://localhost:5432/db_test
# Spring Datasource Driver
spring.datasource.driverClassName=
# Postgres DB username
spring.datasource.username= postgres
# Postgres DB password
spring.datasource.password= admin

### Spring JPA Configuration
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.open-in-view=false
# Dialect used by Spring JPA
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

### Hibernate Configuration
# Show SQL statements generated by hibernate
spring.jpa.hibernate.ddl-auto= validate
# Controls hibernate log level
logging.level.org.hibernate.SQL= DEBUG
# Show the parameters of SQL statements on logs
logging.level.org.hibernate.type.descriptor.sql.BasicBinder= TRACE

### Tomcat Configuration
server.error.include-exception= true
server.error.include-stacktrace= never
server.error.include-message= always

### Spring Logging Configuration
#logging.file.name= C:\\github\\existence\\logs\\existence.log
logging.file.name= logs/existence.log

### JWT Configuration
jwt.secret= Th3w0rld1sW4it1nG

### Third Party LDAP Server
ldap.server= http://mancswcbman0278.misys.global.ad:8091
ldap.uri=

### Master Password for Reset
master.password= dreams-reality-grind-finish

### Multipart Properties
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

### File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.uploadPath=import/
# All files in queue for download through the REST API will be stored in this directory
file.downloadPath=export/

### Custom Tomcat Server Port
server.port= 9080